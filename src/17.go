def find_smallest_negative_number(numbers):
    """
    This function takes a list of numbers and returns the smallest negative number.
    
    Example usage:
    >>> find_smallest_negative_number([10, -2, 3, 5])
    -2
    """
    return min(numbers)
